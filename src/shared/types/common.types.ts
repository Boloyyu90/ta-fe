// src/shared/types/common.types.ts

/**
 * COMMON UTILITY TYPES
 *
 * Previously contained unused base entity and utility types.
 * Cleaned up in type audit - add new shared types here as needed.
 */

// This file is intentionally minimal.
// Add shared types here when genuinely needed across 3+ features.
